# File: frontend/pubspec.yaml

name: sistem_humas_poltek
description: Sistem Informasi HUMAS Politeknik Siber dan Sandi Negara
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.1
  
  # HTTP Client
  dio: ^5.4.0
  
  # Secure Storage
  flutter_secure_storage: ^9.0.0
  
  # Shared Preferences
  shared_preferences: ^2.2.2
  
  # UI Components
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  
  # Form & Validation
  email_validator: ^2.1.17
  
  # Date & Time
  intl: ^0.18.1  # ← HANYA 1x INI SAJA, HAPUS YANG DI BARIS 11
  
  # Loading & Dialogs
  flutter_spinkit: ^5.2.0
  
  # Icons
  cupertino_icons: ^1.0.6
  font_awesome_flutter: ^10.6.0
  
  # Navigation
  go_router: ^13.0.0
  
  # Utilities
  logger: ^2.0.2+1
  path_provider: ^2.1.1
  file_picker: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
