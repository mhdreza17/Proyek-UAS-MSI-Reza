Future<void> openDocumentFromBase64Impl({
  required String base64Data,
  required String fileName,
  required String mimeType,
}) async {
  throw UnsupportedError('File viewing is only supported on web.');
}
